{
  "name": "ping-server",
  "version": "1.0.0",
  "type": "module",          // keeps ES-module syntax working
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {               // optional but recommended
    "node": ">=18"
  }
}
